# Contracts

No new backend API endpoints are introduced in this feature.
State management is handled client-side via `zustand`.
Existing API `/api/sandbox/${threadId}/config.json` is used for saving data.
