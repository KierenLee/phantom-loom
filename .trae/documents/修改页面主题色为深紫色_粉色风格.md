我将根据图片中的视觉风格，修改 `app/globals.css` 中的颜色变量。主要针对暗色模式（Dark Mode）进行调整，因为图片展示的是深色背景。

**计划修改的颜色方案 (基于 OKLCH 颜色空间):**

1.  **主背景色 (`--background`)**: 从深灰蓝改为 **深紫色** (类似 `oklch(0.12 0.03 295)`)，以匹配图片中的黑色/深紫背景。
2.  **主色调 (`--primary`)**: 从浅色改为 **亮粉色** (类似 `oklch(0.65 0.22 345)`)，用于匹配 "INSPIRE ME!" 按钮和星星图标的颜色。
3.  **侧边栏背景 (`--sidebar`)**: 调整为与主背景协调的 **深紫色** (类似 `oklch(0.15 0.04 295)`)。
4.  **卡片/输入框背景 (`--card`, `--input`)**: 调整为 **稍浅的紫灰色** (类似 `oklch(0.2 0.04 290)`)，对应图片中的输入框区域。
5.  **边框颜色 (`--border`)**: 调整为低对比度的 **暗紫色**，保持整体融合。

**执行步骤:**

1.  编辑 `/Users/bytedance/Project/phantom-loom/app/globals.css`。
2.  更新 `.dark` 选择器下的 CSS 变量值。
3.  (可选) 如果需要，也会微调 `:root` 中的默认颜色，或者确保您的应用默认启用暗色模式。